<div class="max-w-4xl mx-auto p-6">
  <div class="bg-black border border-gray-800 rounded-lg shadow-lg overflow-hidden">
    <!-- Header -->
    <div class="px-6 py-4 border-b border-gray-800">
      <div class="flex items-center justify-between">
        <div>
          <h1 class="text-2xl font-bold text-white"><%= @user.username %> is Following</h1>
          <p class="text-gray-400">Following <%= pluralize(@following.count, 'user') %></p>
        </div>
        <%= link_to "← Back to Profile", user_profile_path(@user.username), class: "bg-gray-700 hover:bg-gray-600 text-white font-bold py-2 px-4 rounded" %>
      </div>
    </div>

    <!-- Following List -->
    <div class="divide-y divide-gray-800">
      <% if @following.any? %>
        <% @following.each do |followed_user| %>
          <div class="px-6 py-4 flex items-center justify-between hover:bg-gray-900">
            <div class="flex items-center space-x-4">
              <div class="w-12 h-12 bg-gray-700 rounded-full flex items-center justify-center text-lg font-bold text-white">
                <%= followed_user.username.first.upcase if followed_user.username.present? %>
              </div>
              <div>
                <%= link_to user_profile_path(followed_user.username), class: "text-lg font-semibold text-white hover:text-blue-400" do %>
                  <%= followed_user.username %>
                <% end %>
                <% if followed_user.team.present? %>
                  <p class="text-sm text-gray-400"><%= followed_user.team.name %> fan</p>
                <% end %>
              </div>
            </div>
            
            <% if Current.user != followed_user %>
              <div class="flex space-x-2">
                <div id="follow_button_<%= followed_user.id %>">
                  <% if Current.user.following?(followed_user) %>
                    <%= button_to "Unfollow", user_follow_path(followed_user), method: :delete, 
                        class: "bg-red-600 hover:bg-red-700 text-white font-bold py-1 px-3 rounded text-sm border-0",
                        data: { turbo_stream: true } %>
                  <% else %>
                    <%= button_to "Follow", user_follow_path(followed_user), method: :post, 
                        class: "bg-blue-600 hover:bg-blue-700 text-white font-bold py-1 px-3 rounded text-sm border-0",
                        data: { turbo_stream: true } %>
                  <% end %>
                </div>
              </div>
            <% end %>
          </div>
        <% end %>
      <% else %>
        <div class="px-6 py-8 text-center">
          <p class="text-gray-400 text-lg">Not following anyone yet.</p>
        </div>
      <% end %>
    </div>


  </div>
</div>