<!-- Block/Unblock Option -->
<% if Current.user.blocked?(user) %>
  <%= button_to user_unblock_path(user), method: :delete,
      data: { turbo_stream: true },
      class: "block w-full text-left px-3 py-2 text-sm text-green-400 hover:bg-gray-800 rounded" do %>
    ✅ Unblock @<%= user.username %>
  <% end %>
<% else %>
  <%= button_to user_block_path(user), method: :post,
      data: { turbo_stream: true, confirm: "Are you sure you want to block #{user.username}?" },
      class: "block w-full text-left px-3 py-2 text-sm text-red-400 hover:bg-gray-800 rounded" do %>
    🚫 Block @<%= user.username %>
  <% end %>
<% end %>

<!-- Report Option -->
<%= link_to new_report_path(reportable_type: "User", reportable_id: user.id), 
    data: { turbo_frame: "modal" },
    class: "block px-3 py-2 text-sm text-red-400 hover:bg-gray-800 rounded" do %>
  🚩 Report @<%= user.username %>
<% end %>