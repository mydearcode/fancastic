<%= turbo_stream.replace "post_#{@post.id}" do %>
  <%= render 'post', post: @post %>
<% end %>

<%= turbo_stream.remove "post_#{@existing_repost.id}" %>

<%= turbo_stream.replace "flash" do %>
  <%= render 'shared/flash', notice: "Repost removed successfully!" %>
<% end %>